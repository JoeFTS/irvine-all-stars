# Session Log — 2026-03-23

## What Was Done

### 1. Testing Guide & Board Email
- Created PDF testing guide (`testing/Irvine-AllStars-Testing-Guide.pdf`) with instructions for parent, coach, and admin testers
- Created board email (`testing/board-email.md`) — copy-paste ready, invite-only instructions
- Updated both to reflect invite-only access (no self-signup)
- Added note that random images are fine for uploads, test players are preloaded

### 2. Multi-Child Parent Invites
- Admin can now add multiple children when inviting a parent (dynamic "Add Another Child" button)
- API creates one invite record per child, sends one email mentioning all children
- On signup, all siblings are auto-registered in one go
- Bulk CSV upload still works as before (one row per child)

### 3. Security: Score Sheet & Medical Release Downloads Fixed
- Downloads were broken after adding API auth (plain `<a href>` links don't pass auth tokens from localStorage)
- Converted all 4 download links to `fetch()` with Authorization header + blob download
- Fixed in: coach/scores, coach/checklist, admin/scores (blank + per-division)

### 4. Tournament Rules — Linked Rule Books
- "Applicable Rule Books" section now has clickable links opening in new tabs
- Linked: 2026 PONY Baseball Rule Book PDF, 2025 MLB Rules PDF, West Zone Supplemental Rules for Shetland/Pinto MP/Pinto KP

### 5. Coach + Parent Dual Role
- Admin can check "This coach also has a child in All-Stars" when inviting a coach
- Creates coach invite + parent invite records, sends one email
- On signup, coach gets both portals and child registrations are auto-created
- Nav already shows both "Coach Portal" and "Parent Portal" for coaches
- Manually added Little Elvin (8U KP-Pinto) for coach elvinjaurigue@gmail.com

### 6. Duplicate Registration Bug Fix (Gillette)
- Root cause: case-sensitive `.eq()` matching + email casing mismatch (capital vs lowercase)
- Deleted 2 duplicate partial registrations for Garrett and James Gillette
- Fixed all player name matching to use `.ilike()` (case-insensitive)
- All emails now lowercased on insert

### 7. VPS Deployment
- Saved deployment instructions to memory (SSH alias `vps`, IPv6, build + copy + PM2 restart)
- Deployed 6 times this session

## Commits
- `0d2b3e9` — Add multi-child support to parent invites
- `fc6009c` — Fix score sheet and medical release downloads broken by auth
- `278f52f` — Add links to official PONY rule books on tournament rules page
- `772553a` — Support coach+parent dual role with single invite
- `ebc6b61` — Fix case-sensitive duplicate check causing duplicate registrations

## Remaining Items (from testing/10-known-issues.md)
- LOW-01: Portal "Decline" button shows alert to email manually — no backend action
- LOW-02: Auth redirect goes to /portal instead of back to medical-release
- LOW-03: All medical form fields optional — parent can submit empty form
- LOW-04: Evaluator flow has no authentication (anyone can submit scores) — consider invite tokens

## Other Potential Work
- Board testers will start testing soon — may generate bug reports
- 607 total registrations (600 seeded + 7 real/test) across 12 divisions
- Site deadline was March 17 but still in beta testing phase
