# Session Log — March 16, 2026

## What We Did

### 1. Parent Portal Step-by-Step Gating
- Reordered compliance checklist: Register → Sign Contract → Upload Photo → Upload Birth Cert
- Gated steps 2-4 behind player being "selected" for a team (lock icon + hint text)
- Gated photo + birth cert uploads behind signed contract
- Contract page shows "Not Yet Available" until player is selected
- Documents page shows "Not Yet Available" until contract is signed

### 2. Coach Roster Visibility
- Coach roster now only shows players who are "selected"/"alternate" AND have signed their contract
- Updated empty state wording: "on roster" vs "registered" to avoid confusion with dashboard count

### 3. Duplicate Registration Prevention
- Added duplicate check before inserting: same player name + division + parent email = blocked
- Added "Register Another Child" button on success screen (preserves parent info, clears player fields)
- Cleaned up 2 duplicate Noah Valdez registrations from Supabase

### 4. Bug Fixes (Critical)
- **Contract schema mismatch**: Code was inserting `acknowledged_items` (JSON array) + `parent_user_id`, but DB has 7 individual boolean columns (`acknowledge_eligibility`, etc.) + `parent_signature`. Contract signing would have failed for every parent. Fixed.
- **player_name column doesn't exist**: Contract and documents pages were selecting `player_name` but the table uses `player_first_name` + `player_last_name`. Both pages were silently broken. Fixed with `playerFullName()` helper.
- **created_at column doesn't exist**: Contract and documents pages used `.order("created_at")` but the column is `submitted_at`. Queries were failing. Fixed.
- **player_name undefined in portal checklist**: The compliance section referenced `reg.player_name` which was undefined. Fixed.
- **Static files not copied on deploy**: `.next/static` wasn't being copied to standalone output dir. Updated `start-server.sh` to auto-copy on restart.

### 5. E2E Testing
- Full API-level test of player journey (register → select → contract → documents → coach roster): 11/11 PASS
- Playwright browser test of parent portal (login, dashboard, locked states, sign out): 14/14 PASS
- Playwright browser test of coach portal (all 6 pages, navigation, sign out): 9/9 PASS
- Data integrity test (registrations, assignments, schemas, profiles): 9/9 PASS
- **Total: 43/43 PASS**

### 6. Housekeeping
- Updated `tasks/todo.md` — marked Resend API key and admin account as done
- Saved test account credentials to memory (parent + coach)

## Commits
1. `8d2ca2e` — Add step-by-step gating to parent portal compliance flow
2. `747e721` — Copy static assets in start script to fix standalone deploys
3. `c5e3757` — Prevent duplicate player registrations and add multi-child flow
4. `5271474` — Fix contract signing to match database schema
5. `4bac4dd` — Fix player_name column references in contract and documents pages
6. `4c4ffb5` — Fix created_at → submitted_at column name in contract/documents queries
7. `d42321d` — Clarify coach roster labels: "on roster" not "registered"
