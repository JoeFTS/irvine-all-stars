# Session Log — March 17, 2026

## What We Did

### Phase 6 Priority 1 — ALL COMPLETE (previous session)

#### 1. Supabase Tables Verified
- All 13 tables confirmed with row counts via REST API

#### 2. Storage Bucket Created
- Created `player-documents` bucket + 3 RLS policies

#### 3. Email Verification
- All 4 email endpoints tested, Resend domain verified

#### 4. Admin Bulk-Select Flow Built
- Checkboxes, select-all, bulk action bar, progress bar, batch emails

#### 5. Full E2E Production Test — ALL PASS

---

### Phase 6 Priority 2 — ALL COMPLETE

#### Admin Documents Viewer
- `/admin/documents` — view/download/approve/reject uploaded docs per player
- Stats cards, division filter, search, image preview modal

#### Team Compliance Overview
- `/admin/compliance` — division-by-division tournament readiness dashboard
- Per-player checklist (contract, birth cert, photo), progress bars

#### Bug Fixes
- `document_type` mismatch: teams page checked "photo" but portal uploads "player_photo"
- SEO: OpenGraph metadata added to all 6 server-rendered pages
- Mobile polish: 6 files fixed (touch targets, overflow, responsive sizing)

#### Coach Tryouts Page
- `/coach/tryouts` — coaches see division players, scores, submit recommendations
- `coach_selections` table with RLS (coach sees own, admin sees all)
- Admin tryouts: gold "Coach Pick" badges + filter

---

### Scoring System Overhaul

#### New 1-9 Scale (replaces old variable-max/100 system)
- 6 categories: Hitting, Fielding, Throwing, Running, Effort, Attitude
- Scale: 1, 3, 5, 7, 9 only (9 = highest), max total 54
- Updated DB CHECK constraints from 1-5 to 1-9 on all columns
- Added `attitude` column to `evaluator_scores` table

#### Files Updated for New Scale
- `src/app/admin/scores/page.tsx` — fixed column names (`baserunning`→`running`), /9 bars, /54 total
- `src/app/coach/tryouts/page.tsx` — same fixes, single evaluator display (no averaging)
- `src/app/evaluate/score/page.tsx` — 6 categories, 1-9 scale buttons, attitude added
- `src/app/api/score-sheet/route.ts` — 6 columns, updated template, scoring guide

#### Coach Score Entry (`/coach/scores`)
- Simple 3-step flow: Download template → Fill in scores → Upload file
- Score sheet API supports `?division=` param for pre-filled player names
- Parses CSV and XLSX uploads via `src/lib/score-parser.ts`
- Auto-replaces all previous scores on re-upload (no duplicates)
- File selection with "Upload Scores" / "Replace File" buttons
- Google Sheets instructions (import xlsx, fill scores, export, upload)

---

### Admin Flow Improvements

#### Separated Status Updates from Email Sending
- Setting a player to Selected/Alternate/Not Selected NO LONGER auto-sends emails
- New "Send Email" button in expanded player details (confirms before sending)
- Bulk "Send Emails" button in bulk action bar (separate from bulk status set)
- Shows "Email sent!" indicator after successful send

#### Coach Recommendations
- Fixed 403 RLS error on `coach_selections` (missing GRANT for authenticated/anon)
- "Recommended" badge with X button to instantly remove a recommendation
- Simplified insert payload (removed explicit `selected_at`, uses DB default)

#### Coach Application Auto-Accept
- When admin sends a coach invite, their `coach_applications` status auto-flips to "accepted"
- Fixed thesupplycomp@gmail.com: status → accepted, division → 12U-Bronco

#### Teams Table Fix
- Added missing `coach_email` column (was causing "schema cache" error on team creation)
- Granted authenticated/anon access to teams table

---

### UI Fixes

#### Emoji HTML Entities → SVG Icons
- Coaches page: 3 broken emoji codes replaced with SVG icons in blue circles
- Tryouts page: 5 "What to Bring" items + 4 info cards → SVG icons
- Homepage: 2 quick-link card icons → SVG icons
- No emojis anywhere — all clean SVG

#### Coach Application Form
- Error message now shows at top of form (after progress bar) when validation fails
- Clear message: "Please check all required consent boxes before submitting"
- Tested full e2e: DB insert (201), confirmation email (200) — backend working correctly

---

## Commits (this session)
1. `4e86e21` — Phase 6 P2: admin documents/compliance, coach tryouts, SEO, mobile polish
2. `1fdb5bd` — Coach score entry + scoring system overhaul (1-9 scale, 6 categories)
3. `c4ea9b7` — Fix coach score entry UX: pre-filled templates, tab switching, instructions
4. `5e91212` — Fix score sheet API: division param was hitting session-not-found guard
5. `284cba3` — Remove manual entry tab from coach score entry
6. `636119a` — Simplify coach score entry: 3-step download/fill/upload flow
7. `0c390cb` — Fix score upload: update DB constraints 1-5 to 1-9, add file replace option
8. `e8c306d` — Simplify coach tryouts score display: single evaluator, show notes
9. `a0df473` — Always replace all previous scores on re-upload
10. `1a641be` — Fix coach recommendations: grant RLS access, add remove button
11. `d2cbd72` — Separate status updates from email sending in admin tryouts
12. `6e53016` — Replace broken emoji HTML entities with SVG icons
13. `62fff85` — Improve coach application error visibility
14. `2a38459` — Auto-accept coach application when invite is sent

## Database Changes Made This Session
- Added `attitude` column to `evaluator_scores`
- Updated all CHECK constraints from 1-5 to 1-9 (hitting, fielding, throwing, running, effort, baseball_iq, attitude)
- Created `coach_selections` table with RLS policies
- Added `coach_email` column to `teams` table
- Granted authenticated/anon access to `coach_selections` and `teams` tables
- Updated thesupplycomp@gmail.com: profile division → 12U-Bronco, application status → accepted

## Known Issues / Next Steps
- Homepage still needs real team photos when available
- Analytics (GA4 or Plausible) not yet set up
- Practice Schedule Board, Tournament Hub, Photo Gallery — nice-to-haves
- `joe@fivetoolsolutions.com` test coach application exists in DB (can delete or keep)
