# Session Log — March 17, 2026

## What We Did

### Phase 6 Priority 1 — ALL COMPLETE

#### 1. Supabase Tables Verified
- All 13 tables confirmed with row counts via REST API
- Tables: tryout_registrations (2), tryout_sessions (1), tryout_assignments (2), player_documents (0), player_contracts (0), coach_applications (1), coach_certifications (0), profiles (4), teams (0), evaluator_scores (0), announcements (0), invites (4), tournament_agreements (0)

#### 2. Storage Bucket Created
- Created `player-documents` bucket (was missing)
- Added 3 RLS policies: INSERT, SELECT, UPDATE for authenticated users
- Folder structure: `birth-certs/{reg_id}`, `player-photos/{reg_id}`, `coach-certs/{user_id}/{type}`

#### 3. Email Verification
- Resend domain `irvineallstars.com` verified (confirmed in dashboard)
- RESEND_API_KEY present on VPS
- All 4 email endpoints tested: send-confirmation, send-invite, send-selection, send-tryout-invite
- Confirmation + selection emails sent to bloodkin@me.com

#### 4. Admin Bulk-Select Flow Built
- Added checkboxes to each player row in `/admin/tryouts`
- Select All / Deselect All toggle button
- Bulk action bar with Selected / Alternate / Not Selected buttons
- Confirm dialog before bulk operations
- Progress bar showing done/total + emails sent/failed
- Batch email sending via existing `/api/send-selection` endpoint

#### 5. Full E2E Production Test
- Register player (API insert) → ✅
- Send confirmation email to bloodkin@me.com → ✅
- Admin marks "selected" (status update) → ✅
- Send "You've been selected" email → ✅
- Parent signs contract (insert) → ✅
- Parent uploads birth cert (document insert) → ✅
- All state verified correct → ✅
- Test data cleaned up

### Phase 6 Priority 2 — IN PROGRESS

#### 1. Admin Documents Viewer (DONE)
- Created `/admin/documents` page with full document management
- Lists all selected players with their uploaded documents (birth certs, photos)
- Stats cards: total players, contracts, birth certs, photos
- Division filter + player search
- Expandable player rows showing each document with:
  - Document type, file name, upload date, status badge (pending/approved/rejected)
  - View button (opens image preview modal for images, new tab for PDFs)
  - Download button (signed URL download)
  - Quick approve/reject actions for pending documents
- Image preview modal for inline viewing
- Added "Documents" nav item (FileText icon) to admin sidebar

#### 2. Team Compliance Overview (DONE)
- Created `/admin/compliance` page — dedicated tournament-readiness dashboard
- Overall stats: readiness %, divisions ready, players missing items
- Division-by-division breakdown with progress bars
- Expandable divisions showing per-player checklist (contract, birth cert, photo)
- Green checkmark / gray X for each item per player
- "Tournament Ready" badge when 100% of players in a division have all docs
- Added "Compliance" nav item (ShieldCheck icon) to admin sidebar

#### 3. Bug Fix: Photo Compliance Mismatch
- Fixed `document_type === "photo"` → `"player_photo"` in teams page compliance calc
- Portal uploads as `"player_photo"` but teams page was checking for `"photo"` — stats always showed 0 photos

#### 4. Mobile Polish: Admin Nav
- Made admin mobile bottom tabs horizontally scrollable (was cramped with 9 items)

#### 5. Homepage Images
- Only `hero-aerial.png` exists — homepage design is content-driven (divisions, stats, how-it-works)
- No placeholder images to replace — design works without team photos
- Deferred until real team/player photos are available

#### 6. SEO Meta Tags (in progress via subagent)
- Adding per-page Metadata exports to all server-rendered pages

#### 7. Mobile Polish Pass (DONE)
- Navbar: hamburger touch target → 44px min, announcement bar overflow
- Footer: link touch targets, gap-y for wrapping
- Homepage: stats bar text responsive (3xl→5xl)
- Coach form: input touch targets, button overflow on 320px
- Tryouts page: grid gap/padding tightened
- Portal: key dates column width responsive

#### 8. Coach Tryouts Page (DONE)
- Created `/coach/tryouts` — coaches see all tryout players in their division
- Player list sorted by average evaluator score (highest first)
- Shows: rank, name, positions, bats/throws, age (from DOB), current team, status, avg score
- Expandable details: full player info, parent info, per-evaluator score breakdowns with bar charts
- Coach selection flow: checkbox per player → "Submit Recommendations" button
- Stores selections in new `coach_selections` table (with RLS: coach sees own, admin sees all)
- Summary banner: division, total players, selection count
- Added "Tryouts" nav item (UserCheck icon) to coach layout

#### 9. Admin Tryouts — Coach Picks Integration (DONE)
- Admin tryouts page now fetches `coach_selections` data
- Gold "Coach Pick" badge (★) on players recommended by coaches
- "Coach Picks" filter button to show only recommended players
- Expanded details show coach recommendation info (coach ID, date, notes)

#### 10. Database: coach_selections Table Created
- `irvine_allstars.coach_selections` table with RLS
- Columns: id (uuid PK), registration_id, coach_id, division, notes, selected_at
- RLS: coaches insert/view/delete own selections; admins view all

## Commits
1. `287646d` — Add bulk-select flow for admin player status updates

## Notes
- Email address inconsistency: API sends from `AllStars@irvineallstars.com` but templates reference `AllStars@irvinepony.com` in reply-to. Should consolidate.
- `start-server.sh` tries to start a new process even when PM2 is running. Use `pm2 restart irvineallstars` for deploys.
- `bats`/`throws` DB constraints require lowercase values (right/left/switch)
- `uploaded_by` in player_documents is UUID type (user id), not email
