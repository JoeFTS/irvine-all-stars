# Session Log — March 22, 2026

## What We Did

### 1. Site-Wide Audit & Bug Fixes
- **VPS clean rebuild** — stale build artifacts were causing pages to appear as 404 (Next.js 16 Turbopack issue). Resolved with `rm -rf .next && npm run build`.
- **CRITICAL: `birth_cert` → `birth_certificate` typo** in `coach/page.tsx` — coach dashboard compliance was always showing 0 missing birth certificates. Fixed.
- **Accept selection `file_path: null` error** — `player_documents` table has NOT NULL constraint on `file_path`. Fixed by using empty string for document records without actual files (selection_acceptance, medical_release).
- **Medical release same `file_path: null` error** — same fix applied.
- **Confirm attendance silent failure** — parent portal was updating UI even when API call failed. Now checks response before updating state.
- **Admin status update errors** — added error feedback on coach application status changes and document approval/rejection (was silently failing).
- **`console.log` → `console.warn`** in send-tryout-invite for consistency.

### 2. SEO Improvements
- **OG metadata updated** — "Six divisions from 7U to 12U" → "Twelve divisions from Shetland to Pony", added `og:url` and `twitter` card tags.
- **Added `sitemap.xml`** (`src/app/sitemap.ts`) — 9 public pages.
- **Added `robots.txt`** (`src/app/robots.ts`) — blocks crawling of `/admin/`, `/portal/`, `/coach/`, `/evaluate/`, `/auth/`.

### 3. Baseball Favicon
- Created SVG baseball with red stitching as favicon (`src/app/icon.svg`).
- Generated apple-touch-icon PNG (`src/app/apple-icon.png`).

### 4. Sample Test Documents
- Created `sample-data/` folder with 9 test PDFs for upload testing:
  - `pre-tournament-rules.pdf` — tournament rules document
  - `coaches-agreement.pdf` — coach agreement with signature block
  - `sample-liability-insurance.pdf` — sample insurance cert (landscape, SAMPLE watermark)
  - `medical-release-ethan-rodriguez.pdf` / `sophia-chen.pdf` — two medical release forms
  - `birth-certificate-ethan-rodriguez.pdf` / `sophia-chen.pdf` — cartoon-style birth certs
  - `cert-concussion-mike-thompson.pdf` — concussion training certificate
  - `cert-cardiac-arrest-mike-thompson.pdf` — cardiac arrest training certificate
- Python generator script included (`generate-samples.py`) for creating more.

### 5. Player Contract Viewing (NEW FEATURE)
- **Shared contract data** — extracted `contractSections` and `acknowledgments` to `src/lib/contract-data.ts` (DRY, used by signing form and view page).
- **New `/contract-view?id=<registration_id>`** — printable branded contract view showing:
  - Navy/gold header, player name, division
  - All 10 contract sections with full text
  - All 7 acknowledgments with green checkmarks
  - Planned vacations section
  - Electronic signature in serif font with date/time
  - Print/Save as PDF button
- **New `/coach/contracts`** — coach contracts list page with search, division filter (admin), "View" buttons.
- **Coach sidebar** — added "Contracts" nav item between Roster and Certifications.
- **Admin teams** — added "View Contracts (N)" link in each team card.

### 6. Medical Release Viewing (NEW FEATURE)
- **New `/medical-view?id=<registration_id>`** — printable branded medical release view showing:
  - Allergies, medications, medical conditions
  - Insurance provider and policy number
  - Physician name and phone
  - Emergency treatment authorization
  - Parent signature with date
  - Print/Save as PDF button
- Fixed query: removed `created_at` column (doesn't exist in `player_documents`), replaced `.single()` with `.limit(1)` for better error handling.

### 7. Coach Roster — Clickable Document Badges
- **Birth Cert badge** — clickable when uploaded, opens Supabase signed URL in new tab.
- **Photo badge** — clickable when uploaded, opens Supabase signed URL in new tab.
- **Contract badge** — clickable when signed, opens `/contract-view` in new tab.
- **Medical badge** — was hardcoded as "Pending". Now checks actual `player_documents` for `medical_release` records. Clickable when complete, opens `/medical-view`.
- Medical release now part of "Ready" compliance check (was not included before).
- Small external link icon on clickable badges to indicate interactivity.

### 8. Coach Binder Checklist — Medical View Link
- Medical release items in checklist now have "View" link when complete.

---

## Commits (this session)
1. `dc798aa` — Fix birth_cert typo, add error handling, SEO improvements
2. `84fa50f` — Fix accept selection: file_path NOT NULL constraint
3. `81bdb72` — Add viewable player contracts for coaches and admins
4. `f2d0055` — Make roster doc badges clickable: view birth cert, photo, contract
5. `bad50dd` — Fix medical release: file_path NOT NULL constraint
6. `9dd4e96` — Fix medical release status on coach roster
7. `ec24dbc` — Add medical release viewer for coaches and admins
8. `191d94b` — Fix medical view: use limit(1) instead of single()
9. `b311057` — Fix medical view: remove created_at from select
10. `1f3fa84` — Add baseball favicon and apple-touch-icon

## New Routes Added
- `/contract-view?id=<registration_id>` — printable contract view
- `/coach/contracts` — coach contracts list
- `/medical-view?id=<registration_id>` — printable medical release view
- `/sitemap.xml` — SEO sitemap
- `/robots.txt` — crawler directives

## Known Issues / Next Steps
- **Documents page** (`/portal/documents` or `/admin/documents`) may have inaccuracies — needs review
- **Announcements** — test creating announcements so parents see them on portal
- **Player registration flow** — verify full end-to-end flow works cleanly
- **Team creation** — test creating teams in admin panel
- **Phase 7 deferred items**: Analytics, Photo Gallery, Practice Schedule Board, Tournament Hub — not priority (internal tool focus)
- Pitching log: shown in checklist but no upload mechanism on parent side yet
- Admin "Awaiting Acceptance" badge only shows after email is sent in current session (in-memory)
- Decline flow currently directs parent to email coordinator — not automated
