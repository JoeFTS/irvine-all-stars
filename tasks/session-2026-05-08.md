# Session 2026-05-07 to 2026-05-08 — Affidavit intake + per-team admin compliance

## What got built / fixed

### 1. Lincoln Mooneyham parent invite reissued
Parent had wrong email on file (`chadmooneyham@yahoo.com`, no dot). Updated `tryout_registrations.parent_email` to `chad.mooneyham@yahoo.com`, retired the old unused invite, inserted a fresh `parent` invite token, sent the branded parent welcome via Resend. Reg `8989b683-5bcb-4e0e-87d1-6342cd2e4159` (Bronco 12U Blue / Bailon).

### 2. First signed affidavit intake (Hopp / Mustang 9U White)
Manual end-to-end the first time: split the 3-page PDF with `pdfseparate`, copied bundle + pages to `shared-content/documents/affidavits/9U-Mustang-White-Hopp/`, uploaded all four to `player-documents/team-docs/affidavit-final/9U-Mustang-White-Hopp/`, inserted four `team_documents` rows. Schema gotcha discovered: rows had only `division`, no `team_id`, so divisions with multiple teams (9U-Mustang Hopp/Bernal/Grifka) collided on the latest-per-division lookup.

### 3. team_id migration on team_documents
Migration: `ALTER TABLE irvine_allstars.team_documents ADD COLUMN team_id uuid REFERENCES irvine_allstars.teams(id) ON DELETE SET NULL` plus `team_documents_team_id_idx`. Backfilled the four Hopp rows. Updated coach checklist + admin compliance to scope affidavit lookups by team_id (other team docs — `tournament_rules`, `signed_medical_release`, `insurance_certificate` — remain division-scoped). Admin Team Affidavits section now renders one row per team with team_name as the row label (was: row label collided to whichever team won the division-keyed map). Commit `bb6daca`.

### 4. Per-page affidavit print buttons
Coach checklist: amber "Print Individual Pages" subpanel appears under the affidavit upload widget when `affidavit_page_*` rows exist for the team, with Page 1 / Page 2 / Page 3 buttons. Admin compliance: per-page pill buttons inline under the bundle filename. Commit `840f43b` (initial), `bb6daca` (team-scoped).

### 5. Intake script — `scripts/intake_affidavit.mjs`
Codified the workflow into a one-command tool. Auto-detects the team from PDF text (substring match against `teams.team_name`, longest-match-wins to disambiguate "Mustang 9U" vs "Mustang 9U White"), resolves the head coach's `auth.users.id` for `uploaded_by` (handles the duplicate-profile-rows edge case for `mhopp@hotmail.com` by picking the row whose id matches `auth.users`), splits with `pdfseparate`, uploads to `team-docs/affidavit-final/{team_id}/{ts}-{filename}`, and inserts the 4 rows. Re-submissions replace cleanly: existing rows for `(team_id, document_type)` are deleted along with their storage objects before the new upload. Flags: `--dry-run`, `--team-id <uuid>`. Commit `c0b2522`.

Validated end-to-end on three PDFs:
- `AffidavitHopp.pdf` (corrected resubmission of Mustang 9U White / Hopp)
- `AffidavitBailon.pdf` (Bronco 12U Blue / Bailon)
- (initial Hopp, manual)

### 6. PONY role decoder on coach checklist
Section 4 affidavit area got an amber callout block mapping PONY's three name fields:
- **Manager** = head coach
- **Coach** = first assistant
- **Business Manager** = team parent / treasurer / can also be a third coach

Sits between the section header and the Submit/Walkthrough + Upload widgets. Commit `1e6cca5`.

### 7. Admin compliance — per-team breakdown bug fixes
Two bugs in the per-division compliance breakdown got fixed in sequence:

**(a) Single-team-per-division lump.** Each expanded division was showing all of the division's players under one team header (whichever team won the `coachCertsByDivision` map collision) and one cert status row. Refactored `divisionData` to group by team_id within each division; render one sub-section per team with its own roster, head coach, ready count, and cert status. Commit `5bebbca`.

**(b) Cross-division placements bucketed as Unassigned.** With (a) shipped, players whose `registration.division` ≠ `team.division` (kids playing up or down — 5 such rows in the league) showed up as "Unassigned" in their signup division because the team_id didn't match any team in that division. Fixed by trusting `team_id` over `registration.division`: build a `playersByTeamId` map once, then look up `team.division` to place the player in the correct division section. The "Unassigned" bucket now only catches truly-null `team_id` rows (currently zero per the preformed-teams pattern). Commit `4f93eaf`. Saved feedback memory `feedback_trust_team_id_over_division.md`.

### 8. Admin per-team binder index page
New route `/admin/teams/{teamId}/binder` — compact print-friendly index of all 9 tournament-binder sections in coach-checklist order: pitching log, pre-tournament rules + acknowledgment, insurance, signed affidavit (bundle + per-page splits), per-player medical releases, per-player birth certificates, head-coach concussion + cardiac certs, and assistant coach certifications. Each item shows status badge + view/download for any uploaded document. "Print Binder Index" button strips nav/footer/action buttons via `@media print`. Linked from a "View Binder" pill in the admin compliance team header. Admin uses this when sitting with a coach to assemble the physical binder. Commit `45a43dc`.

## New / updated memories

- `reference_resend_api_key_send_only.md` — current key 401s on GET; deferred full-access key creation
- `feedback_macos_downloads_sandbox.md` — Bash sandbox blocks `/Users/joe/Downloads/`; ask user to move first
- `reference_pdf_tools.md` — pdfseparate / pdftotext / pdfinfo available; qpdf / mutool / gs / pypdf NOT
- `project_supabase_single_env.md` — one Supabase project backs dev + prod; SQL/role flips affect prod immediately
- `project_affidavit_intake_workflow.md` — points at `scripts/intake_affidavit.mjs` as the one-command path
- `feedback_trust_team_id_over_division.md` — group by team_id, not registration.division; kids play up/down

## Production state at end of session

- VPS HEAD: `45a43dc`
- Latest live commits: per-team binder, cross-division grouping fix, per-team compliance breakdown, PONY role decoder, team_id migration + scoped affidavits
- All `team_documents` rows have `team_id` set
- 8 `team_documents` rows: 4 for Mustang 9U White / Hopp, 4 for Bronco 12U Blue / Bailon (bundle + 3 pages each)

## Outstanding / next session

- Resend full-access key — deferred so no programmatic delivery verification yet
- 13 of 15 expected coach affidavits still outstanding
- Pre-tournament rules acknowledgment + insurance cert upload still pending for every team (admin uploads insurance, head coaches acknowledge rules)
